export type Erc20Metadata = {
  /**
   * Token address
   */
  address: string

  /**
   * Name of token
   */
  name: string

  /**
   * Token symbol
   */
  symbol: string

  /**
   * Token decimals
   */
  decimals: number
}
